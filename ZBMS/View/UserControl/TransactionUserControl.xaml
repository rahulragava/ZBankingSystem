<UserControl x:Class="ZBMS.View.UserControl.TransactionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:ZBMS.View.UserControl"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:model="using:ZBMSLibrary.Entities.Model"
             xmlns:converters="using:ZBMS.Util.Converters"
             xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
             xmlns:iconPacks="using:MahApps.Metro.IconPacks"
             xmlns:primitives="using:Microsoft.Toolkit.Uwp.UI.Controls.Primitives"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:TypeToBackgroundConverter x:Key="TransactionTypeToBackgroundConverter" />
        <converters:DateFormatter x:Key="DateTimeConverter" />
    </UserControl.Resources>

    <!--<Grid Background="{ThemeResource BackgroundColor}"
          CornerRadius="10">-->
    <!--<Grid.Resources>

            <DataTemplate x:Key="TransactionData"
                          x:DataType="model:TransactionSummary">
                -->
    <!--<local:TransactionDataControl x:Name="TransactionData"
                                              Id="{x:Bind Id, Mode=OneWay}"
                                              TransactionAmount="{x:Bind Amount, Mode=OneWay}"
                                              Description="{x:Bind Description, Mode=OneWay}"
                                              Status="{x:Bind TransactionType, Mode=OneWay}"
                                              SenderAccountNumber="{x:Bind SenderAccountNumber, Mode=OneWay}"
                                              ReceiverAccountNumber="{x:Bind ReceiverAccountNumber, Mode=OneWay}"
                                              TransactionOn="{x:Bind TransactionOn,Mode=OneWay}"
                           />-->
    <!--
                <controls:DataGrid>
                    <controls:DataGrid.Columns>

                    </controls:DataGrid.Columns>

                </controls:DataGrid>
            </DataTemplate>
        </Grid.Resources>-->
    <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>-->

    <!--<TextBlock Text="S.no"
                   HorizontalAlignment="Center"
                   Margin="0 5 0 0"
                   VerticalAlignment="top"></TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="1"
                   Text="From Account"
                   Margin="0 5 0 0"
                   HorizontalAlignment="Center"
                   VerticalAlignment="top"></TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="2"
                   Text="To Account"
                   Margin="0 5 0 0"
                   HorizontalAlignment="Center"
                   VerticalAlignment="top"></TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="3"
                   Text="Time"
                   Margin="0 5 0 0"
                   HorizontalAlignment="Center"
                   VerticalAlignment="top"></TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="4"
                   Text="Amount"
                   Margin="0 5 0 0"
                   HorizontalAlignment="Center"
                   VerticalAlignment="top"></TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="5"
                   Margin="0 5 0 0"
                   Text="Description"
                   HorizontalAlignment="Center"
                   VerticalAlignment="top"></TextBlock>-->
    <!--<ListView Grid.Row="1"
                  Grid.Column="0"
                  Grid.ColumnSpan="6"
                  SelectionMode="None"
                  Margin="0 10"
                  BorderBrush="Gray"
                  BorderThickness="0 1"
                  x:Name="TransactionListView"
                  ItemsSource="{x:Bind TransactionList,Mode=OneWay}"
                  IsItemClickEnabled="False"
                  ItemTemplate="{StaticResource TransactionData}">
        </ListView>-->
    <Grid 
                  CornerRadius="10"
                  Padding="10"
                  Background="{ThemeResource BackgroundColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Hidden"
                      Grid.Row="1"
                      >
            <controls:DataGrid ItemsSource="{x:Bind TransactionList,Mode=OneWay}"
                               AutoGenerateColumns="False"
                               IsReadOnly="True"
                               IsTabStop="False"
                               Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                               IsDoubleTapEnabled="False"
                               IsTapEnabled="False"
                               RowHeight="60"
                               AlternatingRowBackground="{ThemeResource BackgroundColor}"
                               CanUserResizeColumns="False"
                               >
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="Id"
                                                 CanUserResize="False"  
                                                 Binding="{Binding Id}"
                                                 Width="*">
                        <controls:DataGridTextColumn.HeaderStyle>
                            <Style TargetType="primitives:DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment"
                                        Value="Center" />
                                <Setter Property="BorderThickness"
                                        Value="0" />
                            </Style>
                        </controls:DataGridTextColumn.HeaderStyle>
                        <controls:DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming"
                                        Value="CharacterEllipsis" />
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                            </Style>
                        </controls:DataGridTextColumn.ElementStyle>
                    </controls:DataGridTextColumn>
                    <controls:DataGridTextColumn Header="Sender"
                                                 Width="*"
                                                 Binding="{Binding SenderAccountNumber}"
                                                 ToolTipService.ToolTip="{Binding SenderAccountNumber}">
                        <controls:DataGridTextColumn.HeaderStyle>
                            <Style TargetType="primitives:DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment"
                                        Value="Center" />
                                <Setter Property="BorderThickness"
                                        Value="0" />
                            </Style>
                        </controls:DataGridTextColumn.HeaderStyle>
                        <controls:DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming"
                                        Value="CharacterEllipsis" />
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                            </Style>
                        </controls:DataGridTextColumn.ElementStyle>

                    </controls:DataGridTextColumn>
                    <controls:DataGridTextColumn Header="Receiptant"
                                                 Width="*"
                                                 Binding="{Binding ReceiverAccountNumber}"
                                                 ToolTipService.ToolTip="{Binding ReceiverAccountNumber}"
                                                 >
                        <controls:DataGridTextColumn.HeaderStyle>
                            <Style TargetType="primitives:DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment"
                                        Value="Center" />
                                <Setter Property="BorderThickness"
                                        Value="0" />
                            </Style>
                        </controls:DataGridTextColumn.HeaderStyle>
                        <controls:DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming"
                                        Value="CharacterEllipsis" />
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                            </Style>
                        </controls:DataGridTextColumn.ElementStyle>

                    </controls:DataGridTextColumn>
                    <controls:DataGridTextColumn Header="Transaction On"
                                                 Width="*"
                                                 ToolTipService.ToolTip="{Binding TransactionOn,Converter={StaticResource DateTimeConverter}}"
                                                 Binding="{Binding TransactionOn,Converter={StaticResource DateTimeConverter}}">
                        <controls:DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming"
                                        Value="CharacterEllipsis" />
                                <Setter Property="TextAlignment"
                                        Value="Center" />
                            </Style>
                        </controls:DataGridTextColumn.ElementStyle>
                        <controls:DataGridTextColumn.HeaderStyle>
                            <Style TargetType="primitives:DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment"
                                        Value="Center" />
                                <Setter Property="BorderThickness"
                                        Value="0" />
                            </Style>
                        </controls:DataGridTextColumn.HeaderStyle>

                    </controls:DataGridTextColumn>

                    <controls:DataGridTemplateColumn Header="Amount">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="model:TransactionSummary">
                                <StackPanel Orientation="Horizontal"
                                            CornerRadius="6"
                                            Padding="3 3 4 3"
                                            HorizontalAlignment="Center"
                                            Background="{x:Bind TransactionType, Converter={StaticResource TransactionTypeToBackgroundConverter}}"
                                            VerticalAlignment="Center">
                                    <TextBlock TextAlignment="Center"
                                               Foreground="White"
                                               ToolTipService.ToolTip="{x:Bind Amount,Mode=OneWay}"
                                               VerticalAlignment="top">
                                    <Run Text="&#x20b9;" /> <Run  Text="{x:Bind Amount,Mode=OneWay}"></Run>
                                    </TextBlock>
                                    <!--<TextBlock Padding="5,0,5,0"
                                           Text="{x:Bind  Amount,Mode=OneWay}" />-->
                                </StackPanel>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>

                    <controls:DataGridTextColumn Header="Description"
                                                 Width="*"
                                                 Binding="{Binding Description}"
                                                 ToolTipService.ToolTip="{Binding Description}">
                        <controls:DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming"
                                        Value="CharacterEllipsis" />
                            </Style>
                        </controls:DataGridTextColumn.ElementStyle>
                        <controls:DataGridTextColumn.HeaderStyle>
                            <Style TargetType="primitives:DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment"
                                        Value="Center" />
                                <Setter Property="BorderThickness"
                                        Value="0" />
                            </Style>
                        </controls:DataGridTextColumn.HeaderStyle>

                    </controls:DataGridTextColumn>
                </controls:DataGrid.Columns>

            </controls:DataGrid>

        </ScrollViewer>

        <!--<Border Grid.Row="0"
                HorizontalAlignment="Center"
                BorderBrush="#DEE4EC"
                BorderThickness="1"
                CornerRadius="5"
                Padding="5"
                Background="#ffffff"
                Margin="0 10 10 10">
            <StackPanel Orientation="Horizontal">
                <Button>
                    <iconPacks:PackIconMaterial Kind="ChevronLeft"/>
                </Button>
                <Rectangle Width="1"
                           Fill="#DEE4EC"
                           Margin="3 7" />

                <Button Content="1" />
                <Button Content="2"
                        Background="#7950F2"
                        Foreground="#ffffff" />
                <Button Content="3" />

                <TextBlock Text="..."
                           Foreground="#6C7682"
                           VerticalAlignment="Center"
                           Margin="10 0"
                           FontSize="15" />

                <Button
                        Content="7" />
                <Button
                        Content="8" />
                <Button 
                        Content="9" />

                <Rectangle Width="1"
                           Fill="#DEE4EC"
                           Margin="3 7" />
                <Button>
                    <iconPacks:PackIconMaterial Kind="ChevronRight"
                                          />
                </Button>
            </StackPanel>
        </Border>-->
    </Grid>
    <!--</Grid>-->

</UserControl>
